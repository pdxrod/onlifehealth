Do the following:

$ rvm install 2.1.3
$ rvm use 2.1.3
$ gem install bundler
$ bundle install
$ rvm gemset create onlifehealth
$ rvm use ruby-2.1.3@onlifehealth
$ rspec
(or 
$ FILE=Pitching-07-12.csv rspec 
if you want to use pitching instead of batting data - pitching file not finished)

